# Neon Auth environment variables for Next.js
NEXT_PUBLIC_STACK_PROJECT_ID='xxx'
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY='xxx'
STACK_SECRET_SERVER_KEY='xxx'

# Database owner connection string
DATABASE_URL='xxx'

# Development database URL (optional - if not set, uses DATABASE_URL in development)
# Set this to a separate development database to avoid affecting production data
DEV_DATABASE_URL='xxx'